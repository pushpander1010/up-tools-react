# Changesets

This directory is managed by `@changesets/cli`.

Each changeset is a markdown file describing a version bump. Run `pnpm changeset` to create one.

For PRs that don't need a version bump (docs, CI, refactors), run `pnpm changeset --empty`.

See the [changesets documentation](https://github.com/changesets/changesets) for details.
